# 商业化上线验收清单

更新日期：2026-09-18

这份清单把代码完成、支付安全验收和生产开关分开。正式套餐已经确定，不再进行价格或档位测试；支付签名、金额、幂等、取消和退款仍是不可省略的上线门。没有完成前一层，不进入下一层。

## 1. 代码与迁移

- [x] `npm test -- --run` 全部通过（40 个测试文件、160 项），`npm run build` 通过。
- [x] 新迁移已按顺序完成代码审查：
  - `20260912090000_commercialization_ledger.sql`
  - `20260912091000_extend_product_event_catalog.sql`
  - `20260912100000_global_ai_discovery_dimensions.sql`
  - `20260912102000_payment_checkout_and_fulfillment.sql`
- [x] 在隔离目录取回远端原始迁移历史，dry-run 确认只会应用 `20260913090000_unified_points_wallet.sql` 后完成生产部署；未执行 `migration repair`。
- [x] 已部署支付、钱包、旧 AI 推荐、报告、埋点、管理台及旧前端兼容函数；函数返回不包含密钥、回调原文、照片、面部比例、博主名字或 AI 输出。
- [x] 已部署 `ai-creator-discovery` 退役处理器；公开入口已移除，历史指标与流水只读保留。
- [x] `/report` 每份报告扣 100 积分；Checkout 展示单次报告、月卡和年卡；`/plus` 保留兼容跳转。
- [x] 在工作区把服务端目录和管理台改成唯一 MAKE UP Pro，并增加人工会员管理、幂等发分和最小化审计。
- [x] 已部署会员订阅、商业事件、人工会员管理、正式目录和商业概览迁移；12 个有效旧 Plus 已转换为月卡并各发 1000 积分。

## 2. Stripe 测试环境

- [ ] 配置 `STRIPE_SECRET_KEY`、`STRIPE_WEBHOOK_SECRET` 和 MAKE UP Pro recurring Price；金额、币种和积分由 `membership_plans` 管理，不读取旧 `PAYMENT_*` Secrets。
- [ ] 创建 MAKE UP Pro 订阅，确认订单先为 `pending`，不能只凭 Checkout 返回发放积分。
- [ ] 支付跳回 `/report` 后，`payment-status` 只允许当前账号读取自己的最小订单状态，前端在账期确认后刷新会员与积分。
- [ ] 核对 `checkout.session.completed`、`customer.subscription.*`、`invoice.paid`、`invoice.payment_failed` 和 `invoice.payment_action_required`；只有付款事件与目录金额、币种、周期匹配时发分，年卡后续积分只按月发放。
- [ ] 重放相同 webhook，确认返回成功但不重复增加额度。
- [ ] 测试失败、超时、退款事件和用户取消；代码已支持 Stripe 全额退款事件与管理员全额退款，仍需供应商测试环境验收订单和权益只回滚一次。
- [ ] 确认管理台只显示聚合状态，不显示 provider order id、原始回调或付款凭证。

## 3. ZPAY

- [x] 已核对官方开发文档：`pid`、订单号、通知/回跳地址、商品、金额、支付方式、`sign_type` 和 MD5 排序规则；异步通知要求 `TRADE_SUCCESS` 并返回 `success`。
- [x] 已写入纯函数签名测试、服务端金额/商户号/状态校验、POST 表单提交、支付订单幂等和重复回调幂等路径；商户密钥只从 Edge Function Secrets 读取，管理员退款调用官方 `api.php?act=refund`。
- [x] 已配置 `ZPAY_PID`、`ZPAY_KEY`、`ZPAY_NOTIFY_URL`、`ZPAY_RETURN_URL`，并于 2026-09-18 明确设置 `ZPAY_TYPE=alipay`；未读取或输出密钥值。
- [ ] 完成 ¥19.9 单次报告、¥59 月卡、¥599 年卡、重复通知、失败通知、回跳查询、人工续费、年卡按月发分和退款责任验收。

## 4. 生产开关

- [ ] 临时环境端到端测试通过，并确认客服/退款责任人和支持成本记录方式。
- [x] 生产已部署统一积分、会员、正式目录、管理台迁移及支付函数和 ZPAY Secrets；前端支付开关与源码硬门槛仍为关闭。
- [ ] 第一笔真实订单使用小额、人工盯单；核对到账、权益、报告交付、退款路径后再扩大地区或产品。
- [ ] 每周一 10:00 复核支付、会员发分与报告使用、邀请增长和创作者供给；反馈者符合率不外推整体满意度。

## 当前状态

- 代码路径：`/report` 报告工作区、独立 `/subscription`、MAKE UP Pro Checkout、服务端正式目录和管理台均已发布；测试为 40 个文件、160 项全部通过。
- Stripe：订阅 Checkout、签名 webhook、幂等账期发分和退款回滚函数已部署，生产仍缺少 `STRIPE_*` Secrets 和供应商环境验收。
- ZPAY：一次性提交、签名、回调和管理员全额退款函数已部署；生产已配置所需 Secrets 并明确设置支付宝渠道，真实订单验收尚未完成。
- 独立 AI 推荐：公开入口已移除，HTTP 410 退役处理器已部署，历史指标和流水继续只读保留。
- 前端运行态：截至 2026-09-18，正式域名首页、`/report`、`/subscription`、`/account` 与 `/admin` 均返回 200；未登录页面和 390px 移动端布局已验收，无控制台错误或横向溢出。
- 生产只读核对：正式目录为月卡与年卡各 2 个供应商共 4 条，以及单次报告 2 条；支付入口保持关闭。
- 生产迁移：统一积分、会员、正式目录、旧 Plus 转换、人工会员管理与商业概览迁移已执行。仓库早期迁移时间戳与远端历史仍有分叉，后续部署继续使用隔离的远端原始历史，不能直接从当前目录 push，也不能用 `migration repair` 掩盖分叉。
