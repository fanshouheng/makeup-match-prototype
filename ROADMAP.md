# 路线图

本路线图是当前商业化优先级，不构成功能或时间承诺。积分与支付的详细合同见 `docs/积分与支付系统重构方案.md`。

## 当前状态

- 普通女生匹配继续保留当前浏览器前 3 次免费和邀请所得次数，不消耗积分，也不出售匹配次数。
- 原结果页 3 积分 AI 博主名字推荐已经退役；旧账本用途与匿名指标只保留历史兼容，不再产生新请求。
- 完整妆造报告每次消耗 100 积分，包含公开博主名字线索；创建任务时预占，失败或过期自动退回，成功报告只保存在当前浏览器。
- 正式目录为单次报告（100 积分）、月卡（1000 积分 + 每日 50 次普通匹配）和年卡（每月 2000 积分 + 普通匹配不限次）；积分永久有效，年卡积分按月发放。
- 一次性积分包只保留历史订单兼容，不再公开销售；上线时把仍有效的旧 Plus 统一替换为 1 个月月卡并发 1000 永久积分，旧不限次权益立即结束且不折算原剩余期限。
- 统一积分迁移已部署；`/report`、`/subscription`、单一 MAKE UP Pro 服务端目录和“会员与套餐”管理台均已在工作区完成并通过本地验证。会员订阅与人工管理迁移尚未部署，线上支付开关仍保持关闭；截至 2026-09-16，生产 `/report` 与 `/subscription` 仍返回 404。

## 上线顺序

1. 审查并部署会员订阅、商业事件和人工会员管理迁移，以及对应订阅版 Edge Functions；部署前保持支付关闭。
2. 配置 Stripe recurring Price，完成账期发分、取消续费和失败付款的技术验收。
3. 完成 ZPAY 单次报告、¥59 月卡、¥599 年卡，以及重复回调、人工续费、按月发分、回跳状态和退款技术验收。
4. 补齐用户自助取消续费和账单历史；当前订阅状态与账期已在 `/subscription` 展示。
5. 只在上述上线门全部通过后设置 `VITE_ENABLE_ONLINE_CHECKOUT=true` 并验证生产页面。

## 当前商业验证

不再进行套餐档位或价格 A/B 测试，直接按单一正式方案上线。每周只处理一个最大阻塞点，默认观察：有效访问到结果展示、创作者点击、分享、会员页到首期支付、账期发分、7 天内首次报告生成、报告本机保存、续费、支付失败与退款。主动反馈只用于定位问题，不把反馈者符合率外推为整体满意度；样本不足时不提前宣称留存或 ARPU 成立。

## 创作者与全球化

- 继续优先本人或授权代表提交的创作者资料，并处理超过 7 天的审核积压。
- 报告内返回的公开博主名字只是未核验线索，不得下载候选照片、提取特征或未经授权入库。
- 男生方向先获得并核验足够的美妆博主授权，再决定是否开放匹配；不恢复评价人式趣味报告作为主入口。

## 暂时不做

- 等级、签到、任务中心、积分商城、积分兑换现金或积分转赠；
- 普通匹配次数包、付费排名、广告或赞助推荐；
- 未授权博主抓取、批量照片处理或 AI 名字自动入库；
- 依据主动反馈者比例宣称整体符合率；
- 在支付和退款验收完成前开放生产线上支付。
